---
"@thulite/inline-svg": patch
---

Updates development tooling dependencies to newer versions (Changesets + Prettier), with corresponding lockfile refresh.

Changes:

- Bump `@changesets/changelog-github`, `@changesets/cli`, and `prettier` versions in `package.json`
- Regenerate `package-lock.json` to reflect updated transitive dependency graph
-
